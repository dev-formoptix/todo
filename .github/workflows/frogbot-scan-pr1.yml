name: Upload and Scan Artifacts

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JFrog CLI
      uses: jfrog/setup-jfrog-cli@v4
      with:
        version: 'latest'

    - name: Configure JFrog CLI
      run: |
        jfrog config add my-config --url=${{ secrets.JF_URL }} --apikey=${{ secrets.JF_ACCESS_TOKEN }}

    - name: Run Xray Scan (optional)
      run: |
        jfrog xray scan --url=${{ secrets.JF_URL }} --apikey=${{ secrets.JF_ACCESS_TOKEN }} 
